# ---------------------------------------------
# 📊 DATA PROCESSING & ANALYSIS
# ---------------------------------------------
pandas>=2.2.2          # Data manipulation and analysis — core for working with tabular data
numpy>=2.1.0           # Numerical operations, arrays, and matrix support (used by almost all ML libraries)

# ---------------------------------------------
# 🧠 MACHINE LEARNING
# ---------------------------------------------
scikit-learn>=1.5.0    # Classic ML models (regression, classification, clustering), preprocessing, model evaluation
xgboost>=2.0.3         # Gradient boosting for structured/tabular data — Python 3.13 compatible

# ---------------------------------------------
# 📈 VISUALIZATION
# ---------------------------------------------
matplotlib>=3.9.0      # Base library for plotting graphs, charts, and visualizations
seaborn>=0.13.2        # High-level interface for drawing attractive and informative statistical graphics (built on matplotlib)

# ---------------------------------------------
# 📦 EXPERIMENT TRACKING & MODEL MANAGEMENT
# ---------------------------------------------
mlflow>=2.14.1         # End-to-end ML lifecycle tracking, including experiment tracking, model packaging, and deployment

# ---------------------------------------------
# ✅ TESTING
# ---------------------------------------------
pytest>=8.3.2          # Python testing framework — great for writing unit tests for ML pipelines, data validation, etc.

# ---------------------------------------------
# ⚡ API DEVELOPMENT (FOR MODEL SERVING)
# ---------------------------------------------
fastapi>=0.115.0       # Lightweight, high-performance web framework for serving ML models via REST APIs
uvicorn>=0.30.6        # ASGI server for running FastAPI apps — lightweight and fast

# ---------------------------------------------
# 🧰 MISC
# ---------------------------------------------
pyyaml>=6.0.2          # YAML parsing and configuration support
joblib>=1.4.2          # Lightweight persistence and parallel computing utilities
setuptools>=70.0.0     # Build system support — required for Python 3.13
ipykernel>=6.29.5      # Jupyter kernel for interactive development
